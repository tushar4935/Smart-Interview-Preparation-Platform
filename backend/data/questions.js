// Question bank for the seed script. Kept out of seed.js so the seed logic stays
// readable. Category is attached per-block at the bottom; time limits are derived
// from type/difficulty so we don't repeat them on every line.
const q = (text, difficulty, expectedAnswer, keywords = [], tags = [], type = 'text') =>
  ({ text, difficulty, expectedAnswer, keywords, tags, type });

const dataStructures = [
  q('What is an array and what are its main limitations?', 'Easy', 'An array is a contiguous block of memory storing elements of the same type with O(1) index access. Its main limitation is a fixed size and O(n) insertion/deletion in the middle.', ['array', 'contiguous', 'index', 'fixed size'], ['arrays']),
  q('Explain how a singly linked list differs from an array.', 'Easy', 'A linked list stores nodes with a value and a pointer to the next node, giving O(1) insertion/deletion at known positions but O(n) access. Arrays give O(1) access but costly middle inserts.', ['linked list', 'node', 'pointer', 'access', 'insertion'], ['linked-list']),
  q('What is a hash table and how does it handle collisions?', 'Medium', 'A hash table maps keys to buckets via a hash function for average O(1) lookup. Collisions are handled with chaining (linked lists per bucket) or open addressing (probing for the next free slot).', ['hash table', 'hash function', 'collision', 'chaining', 'open addressing'], ['hashing']),
  q('Explain the difference between a stack and a queue.', 'Easy', 'A stack is LIFO (push/pop from one end), a queue is FIFO (enqueue at back, dequeue from front). Stacks suit undo/recursion, queues suit scheduling and BFS.', ['stack', 'queue', 'LIFO', 'FIFO'], ['stack', 'queue']),
  q('What is a binary search tree and what property must it maintain?', 'Medium', 'A BST is a binary tree where every left descendant is smaller and every right descendant is larger than the node. This gives O(log n) search/insert when balanced, O(n) when skewed.', ['BST', 'binary tree', 'left', 'right', 'balanced'], ['trees', 'bst']),
  q('Explain what a heap is and where it is used.', 'Medium', 'A heap is a complete binary tree satisfying the heap property (min-heap: parent <= children). It backs priority queues and heapsort, with O(log n) insert and extract-min.', ['heap', 'priority queue', 'min-heap', 'complete tree'], ['heap']),
  q('What is a trie and when would you use one?', 'Medium', 'A trie is a tree keyed by characters where paths spell words. It gives O(L) prefix lookups and powers autocomplete and spell-checkers.', ['trie', 'prefix', 'autocomplete', 'characters'], ['trie']),
  q('Reverse a singly linked list in place and explain the approach.', 'Medium', 'Iterate through the list keeping prev, curr, next pointers; on each step point curr.next to prev and advance. Returns the new head in O(n) time and O(1) space.', ['linked list', 'reverse', 'pointers', 'iterative'], ['linked-list'], 'code'),
  q('What is the difference between a graph represented as an adjacency list vs matrix?', 'Medium', 'An adjacency list stores neighbors per node (space O(V+E), good for sparse graphs). A matrix stores a VxV grid (O(V^2) space, O(1) edge lookup), better for dense graphs.', ['graph', 'adjacency list', 'adjacency matrix', 'sparse', 'dense'], ['graphs']),
  q('Explain how a circular queue works and why it is useful.', 'Medium', 'A circular queue wraps the rear index back to the front using modulo, reusing freed slots so a fixed buffer does not waste space. Useful for streaming and ring buffers.', ['circular queue', 'ring buffer', 'modulo', 'rear', 'front'], ['queue']),
  q('What is a doubly linked list and what advantage does it offer?', 'Easy', 'Each node holds pointers to both next and previous nodes, allowing O(1) deletion given a node reference and bidirectional traversal, at the cost of extra memory.', ['doubly linked list', 'previous', 'next', 'bidirectional'], ['linked-list']),
  q('Detect whether a linked list has a cycle.', 'Medium', "Use Floyd's tortoise and hare: advance one pointer by 1 and another by 2; if they ever meet there is a cycle. O(n) time, O(1) space.", ['cycle detection', 'floyd', 'fast slow pointers', 'linked list'], ['linked-list'], 'code'),
  q('What is an AVL tree and how does it stay balanced?', 'Hard', 'An AVL tree is a self-balancing BST where the heights of the two child subtrees differ by at most one. Rotations restore balance after insert/delete, keeping operations O(log n).', ['AVL', 'self-balancing', 'rotation', 'height', 'balance factor'], ['trees']),
  q('Explain the difference between BFS and DFS traversal of a graph.', 'Medium', 'BFS explores level by level using a queue and finds shortest paths in unweighted graphs. DFS goes deep using recursion/stack and suits cycle detection and topological sort.', ['BFS', 'DFS', 'queue', 'stack', 'traversal'], ['graphs']),
  q('What is a self-balancing tree and why does balance matter?', 'Medium', 'Balance keeps a tree height near log n so search/insert/delete stay O(log n). Without it a BST can degrade to a linked list with O(n) operations.', ['balance', 'height', 'log n', 'degrade'], ['trees']),
];

const algorithms = [
  q('Explain binary search and its time complexity.', 'Easy', 'Binary search repeatedly halves a sorted range, comparing the middle element to the target. It runs in O(log n) time but requires the input to be sorted.', ['binary search', 'sorted', 'log n', 'divide'], ['searching'], 'code'),
  q('What is the difference between merge sort and quick sort?', 'Medium', 'Merge sort is stable, always O(n log n), and uses O(n) extra space. Quick sort is in-place with O(n log n) average but O(n^2) worst case; pivot choice matters.', ['merge sort', 'quick sort', 'stable', 'pivot', 'in-place'], ['sorting']),
  q('Explain the two-pointer technique with an example.', 'Medium', 'Two pointers move through a structure from different positions to avoid nested loops, e.g., finding a pair summing to a target in a sorted array in O(n).', ['two pointers', 'sorted', 'pair', 'linear'], ['patterns'], 'code'),
  q('What is dynamic programming and when is it applicable?', 'Hard', 'DP solves problems with overlapping subproblems and optimal substructure by caching subresults (memoization or tabulation), e.g., Fibonacci, knapsack, edit distance.', ['dynamic programming', 'memoization', 'tabulation', 'overlapping subproblems', 'optimal substructure'], ['dp']),
  q('Explain the sliding window technique.', 'Medium', 'A window (subarray/substring) expands and contracts over the input to compute results in O(n) instead of recomputing, e.g., max sum subarray of size k or longest substring without repeats.', ['sliding window', 'subarray', 'substring', 'linear'], ['patterns'], 'code'),
  q('What is a greedy algorithm and where can it fail?', 'Medium', 'A greedy algorithm makes the locally optimal choice at each step. It works when the problem has the greedy-choice property (e.g., interval scheduling) but fails when local choices miss the global optimum (e.g., 0/1 knapsack).', ['greedy', 'local optimum', 'global optimum', 'scheduling'], ['greedy']),
  q('Find the kth largest element in an array efficiently.', 'Medium', 'Use a min-heap of size k (O(n log k)) or quickselect for average O(n). The heap keeps the k largest seen so far; its root is the answer.', ['kth largest', 'heap', 'quickselect', 'partition'], ['heap'], 'code'),
  q("Explain Dijkstra's shortest path algorithm.", 'Hard', "Dijkstra finds shortest paths from a source in a non-negative weighted graph using a priority queue, repeatedly finalizing the closest unvisited node. It runs in O((V+E) log V).", ['dijkstra', 'shortest path', 'priority queue', 'weighted graph'], ['graphs']),
  q('What is recursion and what are its risks?', 'Easy', 'Recursion is a function calling itself with a smaller input toward a base case. Risks include stack overflow from deep/missing base cases and redundant recomputation without memoization.', ['recursion', 'base case', 'stack overflow', 'call stack'], ['recursion']),
  q('Explain how backtracking works with an example.', 'Hard', 'Backtracking builds a solution incrementally and abandons a path as soon as it violates constraints, backtracking to try alternatives, e.g., N-Queens or Sudoku.', ['backtracking', 'constraints', 'prune', 'N-Queens'], ['backtracking']),
  q('What is the time complexity of common operations on a hash map?', 'Easy', 'Insert, delete, and lookup are O(1) on average but O(n) worst case under heavy collisions or a bad hash function. Iteration is O(n).', ['hash map', 'average', 'worst case', 'collision'], ['complexity']),
  q('Given two sorted arrays, merge them into one sorted array.', 'Easy', 'Use two pointers, comparing the front of each array and appending the smaller, until one is exhausted, then copy the rest. O(n+m) time.', ['merge', 'two pointers', 'sorted', 'linear'], ['sorting'], 'code'),
  q('Explain memoization vs tabulation in dynamic programming.', 'Medium', 'Memoization is top-down recursion caching results; tabulation is bottom-up filling a table iteratively. Tabulation avoids recursion overhead; memoization only computes needed states.', ['memoization', 'tabulation', 'top-down', 'bottom-up'], ['dp']),
  q('What is the difference between O(n log n) and O(n^2) and why does it matter?', 'Easy', 'O(n log n) grows far slower than O(n^2); for a million items that is roughly 20 million vs a trillion operations, which is the difference between fast and unusable.', ['big o', 'complexity', 'scalability', 'growth'], ['complexity']),
  q('Detect if a string is a palindrome ignoring case and non-alphanumerics.', 'Easy', 'Normalize the string, then use two pointers moving inward comparing characters. Return false on any mismatch. O(n) time, O(1) extra space.', ['palindrome', 'two pointers', 'string', 'normalize'], ['strings'], 'code'),
];

const systemDesign = [
  q('How would you design a URL shortener like bit.ly?', 'Hard', 'Generate short codes with base62 of an auto-increment id or a hash, store the mapping in a fast KV/DB, cache hot links, and redirect via 301/302. Add analytics and rate limiting.', ['url shortener', 'base62', 'cache', 'redirect', 'key-value'], ['system-design']),
  q('Explain horizontal vs vertical scaling with trade-offs.', 'Medium', 'Vertical scaling adds power to one machine (simple, but a hard ceiling and single point of failure). Horizontal scaling adds more machines behind a load balancer (elastic, but needs statelessness and coordination).', ['horizontal scaling', 'vertical scaling', 'load balancer', 'stateless'], ['system-design']),
  q('What is a load balancer and what strategies can it use?', 'Medium', 'A load balancer distributes traffic across servers to improve availability and throughput using strategies like round-robin, least-connections, or IP-hash, plus health checks.', ['load balancer', 'round robin', 'least connections', 'health check'], ['system-design']),
  q('Explain caching strategies and cache invalidation.', 'Hard', 'Common patterns are cache-aside, read-through, and write-through/write-back. Invalidation uses TTLs or explicit eviction; the hard part is staleness and keeping cache and source consistent.', ['cache-aside', 'write-through', 'TTL', 'invalidation', 'staleness'], ['caching']),
  q('What is the CAP theorem?', 'Hard', 'In a network partition a distributed system can guarantee at most two of Consistency, Availability, and Partition tolerance. Since partitions happen, you effectively choose CP or AP.', ['CAP', 'consistency', 'availability', 'partition tolerance'], ['distributed']),
  q('How would you design a rate limiter?', 'Medium', 'Use token bucket or sliding window counters stored in a fast shared store like Redis, keyed by user/IP. Reject or queue requests over the limit and return 429 with retry info.', ['rate limiter', 'token bucket', 'sliding window', 'redis', '429'], ['system-design']),
  q('Explain the difference between SQL and NoSQL for a system design.', 'Medium', 'SQL gives strong schemas, ACID, and joins, good for relational/transactional data. NoSQL trades those for flexible schemas and horizontal scale, good for high-write or denormalized workloads.', ['sql', 'nosql', 'acid', 'scale', 'schema'], ['databases']),
  q('How do you design a news feed system?', 'Hard', 'Choose fan-out on write (precompute feeds, fast reads, costly for celebrities) vs fan-out on read (compute at request time). Often a hybrid, backed by caches and a ranking service.', ['news feed', 'fan-out', 'ranking', 'cache'], ['system-design']),
  q('What is a message queue and why use one?', 'Medium', 'A message queue (Kafka, RabbitMQ, SQS) decouples producers from consumers, smooths spikes, and enables async processing and retries, improving resilience and scalability.', ['message queue', 'kafka', 'decoupling', 'async', 'backpressure'], ['messaging']),
  q('Explain database sharding and its challenges.', 'Hard', 'Sharding splits data across nodes by a shard key to scale writes/storage. Challenges include choosing a good key, cross-shard joins/transactions, rebalancing, and hotspots.', ['sharding', 'shard key', 'partition', 'hotspot', 'rebalancing'], ['databases']),
  q('What is a CDN and how does it improve performance?', 'Easy', 'A CDN caches static assets on edge servers close to users, reducing latency and origin load. It also absorbs traffic spikes and can mitigate some attacks.', ['CDN', 'edge', 'latency', 'cache', 'static assets'], ['system-design']),
  q('How would you design a real-time chat application?', 'Hard', 'Use WebSockets for bidirectional messaging, a presence service, a message broker/pub-sub for delivery, and a durable store (e.g., Cassandra) for history, sharded by conversation.', ['websocket', 'pub-sub', 'presence', 'chat', 'real-time'], ['system-design']),
  q('Explain idempotency and why it matters in APIs.', 'Medium', 'An idempotent operation has the same effect whether applied once or many times. It matters for safe retries; use idempotency keys so a retried payment or POST is not double-applied.', ['idempotency', 'retry', 'idempotency key', 'safe'], ['api-design']),
  q('What is eventual consistency?', 'Medium', 'In eventual consistency, replicas may temporarily diverge but converge to the same value once writes stop propagating. It buys availability and latency at the cost of reading stale data.', ['eventual consistency', 'replication', 'stale', 'converge'], ['distributed']),
  q('How would you design an image upload and serving service?', 'Hard', 'Store originals in object storage (S3), process thumbnails async via a queue, serve through a CDN, and keep metadata in a DB. Use presigned URLs for direct client uploads.', ['object storage', 'S3', 'thumbnail', 'CDN', 'presigned url'], ['system-design']),
];

const dbms = [
  q('What is database normalization and why is it used?', 'Medium', 'Normalization organizes tables to reduce redundancy and anomalies by splitting data into related tables (1NF-3NF). It improves integrity but can require more joins.', ['normalization', '1NF', '3NF', 'redundancy', 'anomaly'], ['databases']),
  q('Explain the difference between INNER JOIN and LEFT JOIN.', 'Easy', 'INNER JOIN returns only rows matching in both tables. LEFT JOIN returns all rows from the left table plus matches from the right, with NULLs where there is no match.', ['inner join', 'left join', 'null', 'match'], ['sql']),
  q('What is an index and how does it speed up queries?', 'Medium', 'An index (often a B-tree) stores a sorted lookup structure on columns so the DB can find rows without a full scan. It speeds reads but slows writes and uses extra space.', ['index', 'b-tree', 'full scan', 'lookup'], ['sql']),
  q('Explain ACID properties.', 'Medium', 'Atomicity (all-or-nothing), Consistency (valid state transitions), Isolation (concurrent txns do not interfere), Durability (committed data survives crashes).', ['ACID', 'atomicity', 'consistency', 'isolation', 'durability'], ['transactions']),
  q('What is the difference between WHERE and HAVING?', 'Easy', 'WHERE filters rows before grouping; HAVING filters groups after GROUP BY aggregation. HAVING can use aggregate functions, WHERE cannot.', ['where', 'having', 'group by', 'aggregate'], ['sql']),
  q('Write a query to find the second highest salary in a table.', 'Medium', 'SELECT MAX(salary) FROM employees WHERE salary < (SELECT MAX(salary) FROM employees); or use DENSE_RANK() and filter rank = 2 to handle ties.', ['second highest', 'subquery', 'dense_rank', 'max'], ['sql'], 'code'),
  q('What are transactions and isolation levels?', 'Hard', 'A transaction groups statements to run atomically. Isolation levels (read uncommitted, read committed, repeatable read, serializable) trade concurrency for protection against dirty reads, non-repeatable reads, and phantoms.', ['transaction', 'isolation level', 'dirty read', 'phantom', 'serializable'], ['transactions']),
  q('Explain the difference between a primary key and a unique key.', 'Easy', 'A primary key uniquely identifies a row, is not null, and there is one per table. A unique key also enforces uniqueness but allows one null and can appear multiple times per table.', ['primary key', 'unique key', 'null', 'constraint'], ['sql']),
  q('What is a foreign key and what does it enforce?', 'Easy', 'A foreign key references a primary key in another table, enforcing referential integrity so you cannot reference a row that does not exist, and can cascade updates/deletes.', ['foreign key', 'referential integrity', 'cascade', 'reference'], ['sql']),
  q('What is the difference between DELETE, TRUNCATE, and DROP?', 'Medium', 'DELETE removes rows (logged, can filter, can rollback). TRUNCATE removes all rows quickly (minimal logging, resets identity). DROP removes the whole table structure.', ['delete', 'truncate', 'drop', 'rollback'], ['sql']),
  q('Explain what a database view is.', 'Easy', 'A view is a saved query presented as a virtual table. It simplifies complex queries, adds an abstraction/security layer, but is not stored data (unless materialized).', ['view', 'virtual table', 'materialized', 'abstraction'], ['sql']),
  q('What is a deadlock in databases and how do you prevent it?', 'Hard', 'A deadlock is when two transactions each hold a lock the other needs. Prevent it by acquiring locks in a consistent order, keeping transactions short, and using timeouts/deadlock detection.', ['deadlock', 'lock', 'ordering', 'timeout'], ['transactions']),
  q('Explain the difference between SQL and NoSQL databases.', 'Easy', 'SQL databases are relational with fixed schemas and joins; NoSQL databases (document, key-value, column, graph) are schema-flexible and scale horizontally for specific access patterns.', ['sql', 'nosql', 'relational', 'schema', 'document'], ['databases']),
  q('What is denormalization and when is it appropriate?', 'Medium', 'Denormalization intentionally adds redundancy to reduce joins and speed reads, appropriate for read-heavy analytics or caching layers where write complexity is acceptable.', ['denormalization', 'redundancy', 'read-heavy', 'joins'], ['databases']),
  q('How does a GROUP BY with aggregate functions work?', 'Easy', 'GROUP BY collapses rows sharing a column value into one group, and aggregates like COUNT, SUM, AVG compute a value per group. Non-aggregated selected columns must appear in GROUP BY.', ['group by', 'aggregate', 'count', 'sum'], ['sql']),
];

const operatingSystems = [
  q('What is the difference between a process and a thread?', 'Easy', 'A process has its own memory space; a thread runs within a process and shares its memory. Threads are lighter to create and communicate but need synchronization.', ['process', 'thread', 'memory', 'synchronization'], ['os']),
  q('Explain what a deadlock is and the conditions for it.', 'Medium', 'A deadlock is a cycle of processes each waiting on a resource another holds. It needs mutual exclusion, hold-and-wait, no preemption, and circular wait to occur.', ['deadlock', 'mutual exclusion', 'circular wait', 'hold and wait'], ['os']),
  q('What is virtual memory?', 'Medium', 'Virtual memory gives each process a large contiguous address space mapped to physical memory and disk via paging, enabling isolation and running programs larger than RAM.', ['virtual memory', 'paging', 'address space', 'swap'], ['os']),
  q('Explain the difference between paging and segmentation.', 'Hard', 'Paging splits memory into fixed-size pages (no external fragmentation, hides structure). Segmentation splits by logical units of variable size (matches program structure, can fragment).', ['paging', 'segmentation', 'fragmentation', 'page table'], ['os']),
  q('What is a context switch and why is it costly?', 'Medium', 'A context switch saves one process/thread state and loads another so the CPU can multitask. It costs CPU cycles and cache/TLB flushes with no useful work done.', ['context switch', 'state', 'cache', 'scheduler'], ['os']),
  q('Explain process scheduling algorithms.', 'Medium', 'Schedulers pick the next process using algorithms like FCFS, SJF, Round Robin (time slices), and priority scheduling, balancing throughput, latency, and fairness.', ['scheduling', 'round robin', 'FCFS', 'SJF', 'priority'], ['os']),
  q('What is a semaphore and how does it differ from a mutex?', 'Hard', 'A mutex is a lock owned by one thread for mutual exclusion. A semaphore is a counter allowing up to N concurrent accesses and can signal between threads; it has no ownership.', ['semaphore', 'mutex', 'counter', 'ownership', 'synchronization'], ['os']),
  q('Explain the producer-consumer problem.', 'Medium', 'Producers add items to a bounded buffer and consumers remove them; synchronization (semaphores/condition variables) prevents overfilling, underflow, and race conditions.', ['producer consumer', 'bounded buffer', 'semaphore', 'race condition'], ['os']),
  q('What is thrashing?', 'Medium', 'Thrashing is when a system spends more time paging in/out than executing because processes lack enough frames, causing a sharp drop in throughput.', ['thrashing', 'paging', 'frames', 'throughput'], ['os']),
  q('What is the difference between user mode and kernel mode?', 'Easy', 'User mode runs application code with restricted access; kernel mode runs the OS with full hardware access. System calls transition from user to kernel mode.', ['user mode', 'kernel mode', 'system call', 'privilege'], ['os']),
  q('Explain what a system call is.', 'Easy', 'A system call is the controlled interface a program uses to request OS services like file I/O, process creation, or networking, switching into kernel mode.', ['system call', 'kernel', 'interface', 'io'], ['os']),
  q('What are race conditions and how do you prevent them?', 'Medium', 'A race condition is when the result depends on unsynchronized timing of concurrent accesses to shared state. Prevent with locks, atomic operations, or avoiding shared mutable state.', ['race condition', 'synchronization', 'lock', 'atomic'], ['concurrency']),
  q('What is demand paging?', 'Medium', 'Demand paging loads a page into memory only when it is accessed, reducing startup cost and memory use; a page fault triggers the load from disk.', ['demand paging', 'page fault', 'lazy loading', 'disk'], ['os']),
  q('Explain the dining philosophers problem.', 'Hard', 'It models resource contention and deadlock: philosophers need two shared forks to eat. Solutions include ordering resource acquisition or limiting concurrent diners to avoid circular wait.', ['dining philosophers', 'deadlock', 'resource ordering', 'concurrency'], ['os']),
  q('What is the difference between concurrency and parallelism?', 'Easy', 'Concurrency is dealing with many tasks by interleaving progress; parallelism is literally executing multiple tasks at once on multiple cores. Concurrency is structure, parallelism is execution.', ['concurrency', 'parallelism', 'interleaving', 'cores'], ['concurrency']),
];

const networks = [
  q('Explain the difference between TCP and UDP.', 'Easy', 'TCP is connection-oriented, reliable, and ordered with congestion control; UDP is connectionless, unreliable, and low-latency. Use TCP for web/files, UDP for streaming/gaming/DNS.', ['tcp', 'udp', 'reliable', 'connectionless', 'ordering'], ['networking']),
  q('What happens when you type a URL into a browser?', 'Medium', 'DNS resolves the domain to an IP, a TCP (and TLS) connection is established, the browser sends an HTTP request, the server responds, and the browser parses and renders the page.', ['dns', 'tcp', 'tls', 'http', 'rendering'], ['networking']),
  q('Explain the TCP three-way handshake.', 'Medium', 'The client sends SYN, the server replies SYN-ACK, and the client sends ACK, establishing sequence numbers and a reliable connection before data flows.', ['handshake', 'SYN', 'ACK', 'tcp', 'connection'], ['networking']),
  q('What is DNS and how does resolution work?', 'Easy', 'DNS maps domain names to IP addresses. A resolver queries root, TLD, and authoritative servers (often cached) to return the IP for a hostname.', ['dns', 'resolver', 'authoritative', 'cache', 'ip'], ['networking']),
  q('Explain the difference between HTTP and HTTPS.', 'Easy', 'HTTPS is HTTP over TLS, encrypting traffic and authenticating the server via certificates, protecting against eavesdropping and tampering. HTTP is plaintext.', ['http', 'https', 'tls', 'encryption', 'certificate'], ['networking']),
  q('What are the layers of the OSI model?', 'Medium', 'Physical, Data Link, Network, Transport, Session, Presentation, Application. Each layer serves the one above and abstracts the one below.', ['osi', 'layers', 'transport', 'network', 'application'], ['networking']),
  q('What is the difference between a switch and a router?', 'Medium', 'A switch forwards frames within a LAN using MAC addresses (layer 2). A router forwards packets between networks using IP addresses (layer 3) and chooses paths.', ['switch', 'router', 'MAC', 'IP', 'layer'], ['networking']),
  q('Explain what a subnet mask does.', 'Medium', 'A subnet mask splits an IP into network and host portions, defining which addresses are on the same local network and enabling routing and subnetting.', ['subnet', 'mask', 'network', 'host', 'CIDR'], ['networking']),
  q('What is the difference between symmetric and asymmetric encryption?', 'Medium', 'Symmetric uses one shared key (fast, key distribution problem). Asymmetric uses a public/private key pair (slower, solves key exchange). TLS uses asymmetric to exchange a symmetric key.', ['symmetric', 'asymmetric', 'public key', 'private key', 'tls'], ['security']),
  q('Explain how HTTP cookies and sessions work.', 'Easy', 'The server sets a cookie the browser returns on each request. A session id in the cookie maps to server-side session data, keeping users logged in across stateless HTTP requests.', ['cookie', 'session', 'stateless', 'session id'], ['networking']),
  q('What is a socket?', 'Easy', 'A socket is an endpoint (IP + port) for network communication that lets processes send/receive data over TCP or UDP.', ['socket', 'ip', 'port', 'endpoint'], ['networking']),
  q('What is the difference between latency and bandwidth?', 'Easy', 'Latency is the time for one packet to travel (delay); bandwidth is how much data can transfer per second (capacity). Low latency and high bandwidth are both desirable.', ['latency', 'bandwidth', 'delay', 'throughput'], ['networking']),
  q('Explain what an SSL/TLS handshake does.', 'Hard', 'It authenticates the server (and optionally client) via certificates, negotiates cipher suites, and establishes a shared symmetric session key for encrypted communication.', ['tls', 'handshake', 'certificate', 'cipher suite', 'session key'], ['security']),
  q('What is CORS and why does it exist?', 'Medium', 'CORS is a browser mechanism that uses response headers to allow a page from one origin to call a different origin. It exists to relax the same-origin policy safely.', ['CORS', 'origin', 'same-origin policy', 'headers', 'preflight'], ['web']),
  q('What is the difference between GET and POST requests?', 'Easy', 'GET retrieves data, is idempotent, and puts params in the URL; POST sends data in the body to create/change state and is not idempotent. GET is cacheable, POST generally is not.', ['GET', 'POST', 'idempotent', 'body', 'cache'], ['web']),
];

const oop = [
  q('What are the four pillars of OOP?', 'Easy', 'Encapsulation (bundling data and behavior, hiding internals), Abstraction (exposing essentials), Inheritance (reusing via parent-child), and Polymorphism (one interface, many forms).', ['encapsulation', 'abstraction', 'inheritance', 'polymorphism'], ['oop']),
  q('Explain the difference between composition and inheritance.', 'Medium', 'Inheritance models an is-a relationship and reuses a base class; composition models a has-a relationship by holding other objects. Composition is more flexible and favored to avoid rigid hierarchies.', ['composition', 'inheritance', 'is-a', 'has-a'], ['oop']),
  q('What is polymorphism? Give an example.', 'Medium', 'Polymorphism lets different types respond to the same interface, e.g., a draw() method behaving differently for Circle and Square. It includes overriding (runtime) and overloading (compile-time).', ['polymorphism', 'overriding', 'overloading', 'interface'], ['oop']),
  q('Explain encapsulation and why it matters.', 'Easy', 'Encapsulation hides an object internal state behind methods, exposing a controlled interface. It reduces coupling, protects invariants, and lets internals change without breaking callers.', ['encapsulation', 'access modifiers', 'invariants', 'coupling'], ['oop']),
  q('What is an abstract class vs an interface?', 'Medium', 'An abstract class can hold state and partial implementation and is single-inherited; an interface declares a contract with no state (traditionally) and supports multiple implementation.', ['abstract class', 'interface', 'contract', 'multiple inheritance'], ['oop']),
  q('Explain the SOLID principles briefly.', 'Hard', 'Single responsibility, Open/closed, Liskov substitution, Interface segregation, and Dependency inversion, guidelines that keep OO code modular, extensible, and testable.', ['SOLID', 'single responsibility', 'open closed', 'liskov', 'dependency inversion'], ['oop']),
  q('What is method overriding vs overloading?', 'Easy', 'Overriding redefines an inherited method with the same signature (runtime polymorphism). Overloading defines multiple methods with the same name but different parameters (compile-time).', ['overriding', 'overloading', 'signature', 'polymorphism'], ['oop']),
  q('Explain the Liskov Substitution Principle.', 'Hard', 'Subtypes must be usable anywhere their base type is expected without breaking behavior. Violations occur when a subclass weakens guarantees, e.g., a Square subclass breaking Rectangle assumptions.', ['liskov', 'substitution', 'subtype', 'contract'], ['oop']),
  q('What is a design pattern? Name a few.', 'Medium', 'Design patterns are reusable solutions to common problems, e.g., Singleton, Factory, Observer, Strategy, and Decorator. They provide shared vocabulary and proven structure.', ['design pattern', 'singleton', 'factory', 'observer', 'strategy'], ['oop']),
  q('Explain the Singleton pattern and its downsides.', 'Medium', 'Singleton ensures a class has one instance with global access. Downsides include hidden dependencies, global state, and difficulty testing/mocking.', ['singleton', 'global state', 'instance', 'testing'], ['oop']),
  q('What is dependency injection?', 'Medium', 'Dependency injection supplies an object dependencies from outside rather than creating them internally, improving testability, flexibility, and adherence to dependency inversion.', ['dependency injection', 'inversion of control', 'testability', 'decoupling'], ['oop']),
  q('What is the difference between a class and an object?', 'Easy', 'A class is a blueprint defining structure and behavior; an object is a concrete instance of a class with its own state.', ['class', 'object', 'instance', 'blueprint'], ['oop']),
  q('Explain the Observer pattern.', 'Medium', 'The Observer pattern lets subjects notify a list of dependent observers of state changes automatically, decoupling publishers from subscribers, e.g., event systems.', ['observer', 'subject', 'publish subscribe', 'events'], ['oop']),
  q('What is coupling and cohesion?', 'Medium', 'Coupling is how dependent modules are on each other; cohesion is how focused a module responsibilities are. Good design aims for low coupling and high cohesion.', ['coupling', 'cohesion', 'modularity', 'design'], ['oop']),
  q('What is the Factory pattern?', 'Medium', 'The Factory pattern delegates object creation to a method/class so callers depend on an interface rather than concrete classes, easing extension and decoupling instantiation.', ['factory', 'creation', 'interface', 'decoupling'], ['oop']),
];

const javascript = [
  q('Explain closures in JavaScript with an example.', 'Medium', 'A closure is a function that retains access to variables from its defining scope after that scope has returned, e.g., a counter function keeping a private count.', ['closure', 'scope', 'lexical', 'private'], ['javascript']),
  q('What is the difference between var, let, and const?', 'Easy', 'var is function-scoped and hoisted; let and const are block-scoped with a temporal dead zone. const cannot be reassigned (but objects it points to can mutate).', ['var', 'let', 'const', 'scope', 'hoisting'], ['javascript']),
  q('Explain the event loop and how async code runs.', 'Hard', 'The event loop runs the call stack, then drains the microtask queue (promises) and one macrotask (timers, I/O) per tick, enabling non-blocking async on a single thread.', ['event loop', 'microtask', 'macrotask', 'call stack', 'async'], ['javascript']),
  q('What is the difference between == and ===?', 'Easy', '== compares with type coercion; === compares value and type without coercion. Prefer === to avoid surprising conversions.', ['equality', 'coercion', 'strict', 'type'], ['javascript']),
  q('Explain promises and async/await.', 'Medium', 'A promise represents a future value with then/catch. async/await is syntactic sugar that lets you write promise-based code sequentially with try/catch for errors.', ['promise', 'async', 'await', 'then', 'catch'], ['javascript']),
  q('What is hoisting in JavaScript?', 'Medium', 'Hoisting moves declarations to the top of their scope at compile time. Function declarations and var are hoisted (var initialized to undefined); let/const are hoisted but not initialized.', ['hoisting', 'declaration', 'var', 'temporal dead zone'], ['javascript']),
  q('Explain the "this" keyword and how it is determined.', 'Hard', 'this depends on call site: default/undefined in plain calls, the object in method calls, the bound value with bind/call/apply, and the enclosing scope for arrow functions.', ['this', 'binding', 'arrow function', 'call apply bind'], ['javascript']),
  q('What is the difference between null and undefined?', 'Easy', 'undefined means a variable has been declared but not assigned; null is an explicit "no value" assignment. typeof undefined is "undefined", typeof null is "object".', ['null', 'undefined', 'typeof', 'value'], ['javascript']),
  q('Explain prototypal inheritance.', 'Medium', 'Objects link to a prototype object via their [[Prototype]]; property lookups walk the prototype chain. Constructors and classes set up this chain for shared methods.', ['prototype', 'inheritance', 'chain', '__proto__'], ['javascript']),
  q('What are higher-order functions?', 'Easy', 'A higher-order function takes functions as arguments or returns a function, e.g., map, filter, reduce, or a function that returns a configured handler.', ['higher-order', 'map', 'filter', 'reduce', 'callback'], ['javascript']),
  q('Implement a debounce function.', 'Hard', 'Return a function that clears and resets a timer on each call, invoking the callback only after the delay has elapsed without new calls. Useful for search inputs and resize handlers.', ['debounce', 'timer', 'closure', 'setTimeout'], ['javascript'], 'code'),
  q('What is the difference between map, forEach, and reduce?', 'Easy', 'map returns a new transformed array; forEach iterates for side effects and returns nothing; reduce folds the array into a single accumulated value.', ['map', 'forEach', 'reduce', 'array'], ['javascript']),
  q('Explain event delegation.', 'Medium', 'Event delegation attaches one listener to a common ancestor and uses event.target to handle events from many children, improving performance and handling dynamic elements.', ['event delegation', 'bubbling', 'target', 'listener'], ['javascript']),
  q('What is the difference between shallow and deep copy?', 'Medium', 'A shallow copy duplicates the top level but shares nested references; a deep copy duplicates everything recursively. structuredClone or a recursive clone makes a deep copy.', ['shallow copy', 'deep copy', 'reference', 'structuredClone'], ['javascript']),
  q('Explain how "async" iteration and generators work.', 'Hard', 'Generators pause/resume with yield, producing values lazily. Async generators combine this with promises so for-await-of can consume streamed asynchronous values.', ['generator', 'yield', 'async iterator', 'for-await-of'], ['javascript']),
];

const react = [
  q('What is the Virtual DOM and how does React use it?', 'Easy', 'The Virtual DOM is an in-memory representation of the UI. React diffs the new tree against the old (reconciliation) and applies the minimal set of real DOM updates.', ['virtual dom', 'reconciliation', 'diffing', 'render'], ['react']),
  q('Explain the difference between state and props.', 'Easy', 'Props are read-only inputs passed from a parent; state is local, mutable data managed within a component. Changing either triggers a re-render.', ['state', 'props', 're-render', 'immutable'], ['react']),
  q('What are React hooks and why were they introduced?', 'Medium', 'Hooks (useState, useEffect, etc.) let function components use state and lifecycle features, enabling logic reuse via custom hooks without class components.', ['hooks', 'useState', 'useEffect', 'function component'], ['react']),
  q('Explain the useEffect hook and its dependency array.', 'Medium', 'useEffect runs side effects after render. The dependency array controls when it re-runs; empty means once on mount, and a cleanup function runs before re-run/unmount.', ['useEffect', 'dependency array', 'cleanup', 'side effect'], ['react']),
  q('What is the difference between controlled and uncontrolled components?', 'Medium', 'Controlled components drive form values through React state; uncontrolled components let the DOM hold the value, read via refs. Controlled gives more control and validation.', ['controlled', 'uncontrolled', 'ref', 'form'], ['react']),
  q('Explain keys in React lists and why they matter.', 'Easy', 'Keys give list items a stable identity so React can match elements across renders and update efficiently. Using array indices as keys can cause bugs on reorder.', ['keys', 'list', 'reconciliation', 'index'], ['react']),
  q('What is the Context API and when should you use it?', 'Medium', 'Context passes data through the tree without prop drilling, good for global-ish values like theme or auth. For complex, frequently-changing state a dedicated store may fit better.', ['context', 'prop drilling', 'provider', 'global state'], ['react']),
  q('Explain useMemo and useCallback.', 'Hard', 'useMemo caches a computed value and useCallback caches a function reference between renders, both keyed by dependencies, to avoid expensive recomputation or unnecessary child re-renders.', ['useMemo', 'useCallback', 'memoization', 'dependencies'], ['react']),
  q('What causes unnecessary re-renders and how do you prevent them?', 'Hard', 'Re-renders come from state/prop/context changes and new object/function identities. Prevent with memo, useMemo/useCallback, stable keys, and splitting components.', ['re-render', 'memo', 'performance', 'reference'], ['react']),
  q('What is the difference between useState and useReducer?', 'Medium', 'useState suits simple independent values; useReducer centralizes complex or interdependent state transitions in a reducer, making updates predictable and testable.', ['useState', 'useReducer', 'reducer', 'state'], ['react']),
  q('Explain what a custom hook is.', 'Medium', 'A custom hook is a function starting with "use" that composes built-in hooks to share stateful logic between components, e.g., useFetch or useLocalStorage.', ['custom hook', 'reuse', 'logic', 'use'], ['react']),
  q('What is React.memo?', 'Medium', 'React.memo memoizes a component so it skips re-rendering when its props are shallowly equal, useful for pure presentational components with stable props.', ['React.memo', 'memoization', 'props', 'pure'], ['react']),
  q('Explain the component lifecycle in function components.', 'Medium', 'Mount, update, and unmount phases are handled through useEffect: run on mount with [], on updates with deps, and cleanup on unmount via the returned function.', ['lifecycle', 'useEffect', 'mount', 'unmount'], ['react']),
  q('What are error boundaries?', 'Medium', 'Error boundaries are components that catch render-time errors in their subtree via componentDidCatch/getDerivedStateFromError and show a fallback UI instead of crashing the app.', ['error boundary', 'fallback', 'componentDidCatch', 'crash'], ['react']),
  q('How does lifting state up work?', 'Easy', 'Lifting state up moves shared state to the closest common ancestor so multiple children read it via props and update it via callbacks, keeping a single source of truth.', ['lifting state', 'shared state', 'props', 'single source of truth'], ['react']),
];

const node = [
  q('What is Node.js and what is it good for?', 'Easy', 'Node.js is a JavaScript runtime on V8 with an event-driven, non-blocking I/O model, well-suited to I/O-heavy, real-time, and API workloads.', ['node', 'v8', 'event-driven', 'non-blocking', 'runtime'], ['nodejs']),
  q('Explain middleware in Express.', 'Easy', 'Middleware are functions with (req, res, next) that run in order to parse bodies, authenticate, log, or handle errors, calling next() to pass control or ending the response.', ['middleware', 'express', 'next', 'pipeline'], ['nodejs']),
  q('How does the Node.js event loop handle asynchronous operations?', 'Hard', 'Node offloads I/O to libuv and the OS; when work completes, callbacks are queued and run by the event loop across phases (timers, poll, check), keeping the main thread non-blocking.', ['event loop', 'libuv', 'callback', 'phases', 'non-blocking'], ['nodejs']),
  q('What is the difference between process.nextTick and setImmediate?', 'Hard', 'process.nextTick callbacks run before the event loop continues (after the current operation); setImmediate runs in the check phase of the next loop iteration.', ['nextTick', 'setImmediate', 'event loop', 'phase'], ['nodejs']),
  q('Explain how you handle errors in async Express routes.', 'Medium', 'Wrap async handlers in try/catch and forward errors with next(err) to a central error middleware, or use an async wrapper that catches rejections automatically.', ['error handling', 'async', 'next', 'middleware'], ['nodejs']),
  q('What are streams in Node.js?', 'Medium', 'Streams process data in chunks (readable, writable, duplex, transform) without loading it all into memory, ideal for large files, uploads, and piping.', ['streams', 'chunk', 'pipe', 'backpressure'], ['nodejs']),
  q('What is the difference between require and import?', 'Easy', 'require is CommonJS, synchronous and dynamic; import is ES modules, statically analyzable and asynchronous-friendly. Node supports both depending on module type.', ['require', 'import', 'commonjs', 'es modules'], ['nodejs']),
  q('How do you secure an Express API?', 'Medium', 'Use helmet for headers, validate/sanitize input, rate-limit, hash passwords, use JWT/HTTPS, restrict CORS, and keep secrets in env vars.', ['helmet', 'validation', 'rate limit', 'jwt', 'cors'], ['security']),
  q('What is JWT and how does it work for authentication?', 'Medium', 'A JWT is a signed token (header.payload.signature) the server issues on login and the client sends on each request; the server verifies the signature to trust its claims without server-side sessions.', ['jwt', 'token', 'signature', 'stateless', 'auth'], ['nodejs']),
  q('Explain clustering in Node.js.', 'Hard', 'The cluster module forks worker processes sharing a port so a single-threaded Node app uses all CPU cores, with the master distributing connections.', ['cluster', 'worker', 'cpu', 'fork', 'scale'], ['nodejs']),
  q('What is the difference between blocking and non-blocking code?', 'Easy', 'Blocking code halts the thread until an operation finishes; non-blocking code returns immediately and uses callbacks/promises so other work proceeds. Node favors non-blocking I/O.', ['blocking', 'non-blocking', 'callback', 'io'], ['nodejs']),
  q('How does Express routing work?', 'Easy', 'Express matches an incoming method and path to a registered route handler, supporting params, query strings, and routers to modularize endpoints.', ['express', 'routing', 'params', 'router'], ['nodejs']),
  q('What are environment variables and why use them?', 'Easy', 'Environment variables hold configuration and secrets outside code (via process.env/.env), keeping credentials out of source control and letting config differ per environment.', ['env', 'process.env', 'secrets', 'config'], ['nodejs']),
  q('Explain how you would handle file uploads in Node.', 'Medium', 'Use multipart parsing (e.g., multer) with disk/memory storage, validate type and size, store the file or push to object storage, and save metadata in the DB.', ['upload', 'multer', 'multipart', 'validation'], ['nodejs']),
  q('What is the purpose of the package.json file?', 'Easy', 'package.json declares project metadata, dependencies and their versions, scripts, and entry point, letting npm install and run the project reproducibly.', ['package.json', 'dependencies', 'scripts', 'npm'], ['nodejs']),
];

module.exports = { dataStructures, algorithms, systemDesign, dbms, operatingSystems, networks, oop, javascript, react, node };
