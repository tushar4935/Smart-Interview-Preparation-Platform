# Screenshots

Real captures from the running app, with the browser chrome cropped out:

- `dashboard.png` — dashboard with stats, score trend, and difficulty breakdown
- `analytics.png` — analytics with weak-area detection and category performance
- `interview.png` — new interview setup (category, difficulty, question count)
- `history.png` — past interviews with scores
- `resume.png` — resume manager ("practice from my resume")
- `home.png` — landing page with feature cards
- `profile.png` — user profile and skills

To refresh one, retake it from the [live app](https://smart-interview-preparation-platfor-theta.vercel.app) and overwrite the file (PNG, ~1600px wide).

Still to add: `demo.gif` — a short screen recording of a full interview + AI feedback.
